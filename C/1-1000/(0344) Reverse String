void swap (char* s, int i, int j);

void reverseString(char* s, int sSize){
    for (int i = 0; i < (sSize / 2); i++)
    {
        swap(s, i, (sSize - 1 - i));
    }
}

void swap (char* s, int i, int j)
{
    char temp = s[i];
    s[i] = s[j];
    s[j] = temp;
}
